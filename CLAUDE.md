@AGENTS.md

## Claude-specific notes

- Design specs for multi-file tools live in `.docs/dev/<tool>.md` (currently
  `.docs/dev/add-notes.md`). Read the relevant spec before modifying such a tool.
- When a tool's interface or behavior changes, update its spec in `.docs/dev/` and add
  a row to the changelog in `README.md` §9.
